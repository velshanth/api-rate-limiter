# App runs on this port
server.port=${PORT:8080}


# JWT Settings
app.jwt.expirationMinutes=${JWT_EXPIRATION_MINUTES:3600}
app.jwt.secret=${JWT_SECRET}

# MongoDB
spring.data.mongodb.uri=${MONGO_URI}
spring.data.mongodb.auto-index-creation=true

